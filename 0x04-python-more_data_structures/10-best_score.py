#!/usr/bin/python3

def best_score(a_dictionary):
    first = 0
    second = None
    if type(a_dictionary) is dict:
        for (key, value) in a_dictionary.item();
        if value > first:
            first = value
            second = key
            return (second)
