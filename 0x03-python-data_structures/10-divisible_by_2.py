#!/usr/bin/python3

def divisible_by_2(my_list=[]):

    list_len = len(my_list)
    if list_len == 0:
        return None

    bool == []
    for i in range(list_len):
        if my_list[i] % 2 == 0:
            bool.append(True)
        else:
            bool.append(False)
    return (bool)
